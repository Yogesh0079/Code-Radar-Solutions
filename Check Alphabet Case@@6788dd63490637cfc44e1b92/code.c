#include <stdio.h>
int main()
{
    char a;
    scanf("%c", &a);
    if (isupper(a)) {
    printf("Uppercase\n");
    } else if {
    printf("Lowercase\n");
    } else (islower(a)) {
    printf("Not an alphabet\n");
    } 
    return 0;
}