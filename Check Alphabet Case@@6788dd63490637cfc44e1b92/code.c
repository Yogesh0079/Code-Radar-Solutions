#include <stdio.h>
int main()
{
    char a;
    scanf("%ch", &a);
    if (isupper(a)) {
    printf("Uppercase");
    } else if {
    printf("Lowercase");
    } else {
    printf("Not an alphabet");
    } 
    return 0;
}